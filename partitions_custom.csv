# CYD_AnimatedPixelClock - 4MB flash, dual-OTA layout.
#
# Standard project layout: two 1.792MB app slots (OTA A/B) plus a 384KB data
# partition. The `spiffs` subtype name is correct even when the partition is
# mounted as LittleFS - that is expected, not a mistake.
#
# The data partition is currently unused: settings live in NVS via Preferences
# and the web UI is served from PROGMEM. It is retained so a filesystem can be
# added (VLW fonts, or reviving the archived custom-animation player) without
# repartitioning and losing saved settings.
#
# Name,     Type, SubType,  Offset,   Size,    Flags
nvs,        data, nvs,      0x9000,   0x5000,
otadata,    data, ota,      0xe000,   0x2000,
app0,       app,  ota_0,    0x10000,  0x1C0000,
app1,       app,  ota_1,    0x1D0000, 0x1C0000,
spiffs,     data, spiffs,   0x390000, 0x60000,
coredump,   data, coredump, 0x3F0000, 0x10000,
